# Bicep labs — 03-compute

Put your .bicep files for this domain's hands-on labs here as you build them.
Deploy with:
  az deployment group create --resource-group <rg-name> --template-file main.bicep

